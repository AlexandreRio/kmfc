static int
accept(ContainerRoot* this, Visitor* visitor)
{
  if (this->groups != NULL)
  {
    hashmap_map* m = (hashmap_map*) this->groups;
    for (int i=0; i<m->table_size; i++)
    {
      Group* g = (Group*) (m->data[i].data);
      g->VT->fptrAccept(g, visitor);
    }
  }
  return visitor->visit(visitor, this->VT->metaClassName(this), this);
}
