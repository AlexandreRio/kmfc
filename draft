
static int
accept(ContainerRoot* this, Visitor* visitor)
{
  hashmap_map* m = (hashmap_map*) this->groups;
  for (int i=0; i<m->table_size; i++)
  {
    Group* g = (Group*) (m->data[i].data);
    ((VisitorJSON*)visitor)->visitor->visit(visitor, g->VT->metaClassName(g), g);
  }
	return ((VisitorJSON*)visitor)->visitor->visit(visitor, this->VT->metaClassName(this), this);
}
